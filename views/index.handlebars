<h1 style="color:blue;"><b>Mongo Scraper!</b></h1>

<button id='scrape' data-toggle="modal" data-target="#exampleModal" type="button" >Scrape New York Times!</button>

<a href="//www.localhost:3000/articles">View My Articles</a>

<!-- Message Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <p>{{data.length}} articles added!</p>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
      </div>
    </div>
  </div>
</div>

{{#each articles}}

<h2 class='articles'>{{this.title}}</h2>
<p class='paragraph'>{{this.summary}}</p>
<a href="{{this.link}}"></a>

{{#if this.ifNewArticle}}
<button id='Save' class='btn btn-primary save' data-title='{{this.title}}' data-summary='{{this.summary}}' data-link='{{this.link}}'>Save</button>



{{else}}
<button  class='btn btn-primary delete' data-id='{{this._id}}'>Delete from Saved</button>    
<button id='Notes' data-toggle="modal" data-target="#exampleModalCenter" class='btn btn-primary'>Article Notes</button>
{{/if}}
<!--Modal note-->

<div class="modal fade" id="exampleModalCenter" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle">Modal title</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        <input class='userNote' max-length='100%'/>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" data-note='{{this.note}}' data-id='{{this._id}}' class="btn btn-primary saveNote">Save changes</button>
      </div>
    </div>
  </div>
</div>

{{/each}}

{{!--
{{#each burger_data}}
{{#unless this.devoured}}
<span>{{this.burger_name}}</span>
<button data-id={{this.Id}} class="btn btn-outline-secondary devour-btn">Devour</button>
<div id="eaten" class="input-group mb-3">

</div>
{{/unless}}
{{/each }}
<br>

--}}

<br>
<script src='./app.js'></script>